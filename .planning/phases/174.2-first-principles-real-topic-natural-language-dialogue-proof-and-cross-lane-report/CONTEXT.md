# Phase 174.2: First-Principles Real-Topic Natural-Language Dialogue Proof And Cross-Lane Report - Context

**Gathered:** 2026-04-14
**Status:** Ready for planning

<domain>
## Phase Boundary

Prove that the public AITP front door can steer the closed first-principles /
code-method baseline through one real natural-language dialogue, then close the
milestone with one honest cross-lane comparative report.

</domain>

<decisions>
## Implementation Decisions

### Real-topic target
- Use the user-requested large-codebase / first-principles / algorithm
  development direction centered on the bounded `LibRPA QSGW`
  deterministic-reduction consistency core.
- Keep the real dialogue tied to the already-closed bounded positive
  first-principles route from `v1.99`, rather than reopening broader QSGW
  convergence or whole-stack closure claims.

### Cross-lane closure
- The report must compare the three real dialogue proofs now requested in
  `v2.0`: formal theory, toy model numerical + derivation, and large codebase
  / first-principles / algorithm development.
- The report must stay physically and operationally honest: record what each
  lane now proves, where bounded operator intervention still exists, and what
  next widening decision remains deferred.

### the agent's Discretion
The exact wording of the fresh first-principles request and the comparative
report format may vary, but they must remain recognizably in the bounded
`LibRPA QSGW` lane and preserve the already-explicit non-claims.

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `run_librpa_qsgw_positive_l2_acceptance.py` is the bounded positive-L2
  first-principles baseline from `v1.99`.
- `run_librpa_qsgw_target_contract_acceptance.py` already anchors the positive
  route to the real codebase, workflow wrapper, validator, and bounded
  consistency evidence.
- `claim:librpa-qsgw-deterministic-reduction-consistency-core` is the
  canonical positive first-principles route already known to land honestly in
  authoritative `L2`.

### Established Patterns
- Real-topic E2E tests should begin from natural language and then prove that
  the route remains bounded and honest all the way through runtime steering and
  repo-local `L2` read paths.
- This milestone is not about widening the first-principles science claim; it
  is about proving real natural-language steering fidelity for the already
  closed bounded lane.

### Integration Points
- Phase `174.2` closes the third and final dialogue leg of milestone `v2.0`.
- This phase also owns the cross-lane report that compares the formal, toy,
  and first-principles dialogue proofs and makes the next widening decision
  explicit.

</code_context>

<specifics>
## Specific Ideas

- Likely natural-language prompt family:
  "Help me study the bounded LibRPA QSGW deterministic-reduction consistency
  result through the public front door, and keep the route tied to the
  already-proved H2O/really_tight iter=10 positive core."
- The cross-lane report should record:
  - the authoritative `L2` unit reached by each lane
  - the steering artifacts that preserved the natural-language request
  - the remaining bounded blocker or explicit non-claim for each lane

</specifics>

<deferred>
## Deferred Ideas

- Do not widen this phase into full QSGW convergence or broad workflow closure.
- Do not reopen positive-L2 closure mechanics already settled in `v1.99`.
- Do not create a new lane outside the three user-requested directions.

</deferred>
